// Shared code-signing defaults for all configurations.
// Override locally (gitignored): copy Config/Signing.local.xcconfig.example → Config/Signing.local.xcconfig
#include? "Signing.local.xcconfig"

DEVELOPMENT_TEAM = 2N55H39FC4
CODE_SIGN_STYLE = Automatic
