import Foundation

/// Shared row limits for Finder Folder History surfaces.
public enum FolderHistoryDisplayLimits {
    /// Hotkey panel and FinderSync toolbar menu (autojump-style quick pick).
    public static let quickPickCount = 9
    /// Command Center / menu-bar popover.
    public static let commandCenterCount = 15
}
